import "../styles/form.css"; // reuse your existing CSS

const Quote = () => {
  return (
    <div className="quote-container">
      <p className="quote-text">
        "Caring for patients beyond prescriptions."
</p>
    </div>
  );
};

export default Quote;
